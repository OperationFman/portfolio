export type ColumnData = {
	heading: string;
	isDeveloping?: boolean;
	tech: string[];
};

export type ConvictionsRow = {
	heading: string;
	blurb: string;
};

export type Skills = {
	heading: string;
	items: string[];
};
