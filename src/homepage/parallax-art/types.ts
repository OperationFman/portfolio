export type ParallaxArtProps = {
	selectedTheme: string;
};

export type AnimationProps = {
	windowHeight: number;
	selectedTheme: string;
	styles: any;
	offSetY: number;
	children?: React.ReactNode;
};
